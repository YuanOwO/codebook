# 計算幾何

-   [計算幾何](#計算幾何)
    -   [計算幾何一些常用功能](#計算幾何一些常用功能)
    -   [凸包 (Convex Hull)](#凸包-convex-hull)
-   [圓 (Circle)](#圓-circle)
    -   [計算圓面積](#計算圓面積)
    -   [兩圓公切線](#兩圓公切線)
    -   [兩圓公切線](#兩圓公切線_1)
-   [其他](#其他)
    -   [皮克定理](#皮克定理)

## 計算幾何一些常用功能

### 用法

宣告 Convex 結構時傳入排序好的點陣列

### 功能

1. `inside()`: 判斷點是否在凸包裡 0: out, 1: on, 2: in
2. `tangent()`: 傳入一根向量 回傳切點的 `idx` (close = true: 近)
3. `tangent2()`: 傳入一點 回傳切線與凸包的交點
4. `intersect()`: 傳入一條線 回傳交點 (2 點 or 1 點 or 沒點)

### 備註

-   注意資料型態
-   Convex 結構宣告時要傳入逆時針的陣列

## 凸包 (Convex Hull)

### 用法

呼叫 `convex_hull()` 傳入「存入所有點的 `vector`」

### 結果

獲得凸包逆時針的點

# 圓 (Circle)

## 計算圓面積

### 用法

1. 初始化 `init(c)` 其中 `c` 為圓的數量
2. 填入圓心與半徑到 `Circle c[N]` 之中
3. 算出答案 `solve()`

### 結果

`Area[i]` 為至少包括 `i` 個圓的覆蓋面積

### 時間複雜度

$O(n^2 \log n)$

## 兩圓公切線

### 參數

-   `c1`: 圓 1 的結構
-   `c2`: 圓 2 的結構
-   `sign1`: `1`: 外公切線; `-1`: 內公切線

### 回傳值

兩圓的公切線

### 備註

-   回傳的 `vector<Line>` 中
-   第一個是圓 1 的切點
-   第二個是圓 2 的切點

## 兩圓公切線

### 參數

-   `c1`: 圓 1 的結構
-   `c2`: 圓 2 的結構
-   `sign1`: `1`: 外公切線; `-1`: 內公切線

### 回傳值

兩圓的公切線

### 備註

-   回傳的 `vector<Line>` 中
-   第一個是圓 1 的切點
-   第二個是圓 2 的切點

# 其他

## 皮克定理

$$
\begin{align*}
    A = i + \frac{b}{2} - 1 \\
    2A = 2i + b - 2 \\
    i = A - \frac{b}{2}+ 1
\end{align*}
$$

其中:

-   $A$: 面積
-   $b$: 圖形內部的點數量
-   $i$: 圖形線上的點數量

### 用法

先推公式，然後需要哪個就丟哪個函式
