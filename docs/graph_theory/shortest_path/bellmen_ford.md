## Bellmen-Ford Algorithm

### 用途

用每一條邊
找單源最短路徑

### 用法

呼叫 `bellmen_ford(n, m)` 其中 `n` 為節點數 `m` 為邊數

### 結果

`dis[i]`: 從起點到 `i` 的最短路徑長

### 時間複雜度

$O(VE)$，其中 $E$ 是邊數，$V$ 是節點數

### 備註

-   注意資料型態
-   一般來說，沒負環的話跑 `n - 1` 次就好
    如果跑到第 `n` 次仍鬆弛的到東西，那就是有負環
