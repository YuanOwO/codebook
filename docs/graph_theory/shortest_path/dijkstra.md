## Dijkstra's Algorithm

### 用途

找單源最短路徑

### 用法

呼叫 `dijkstra(s)` 其中 `s` 為起點

### 結果

回傳 `dis[i]`: 從 `s` 到 `i` 的最短路徑長

### 時間複雜度

$O(E + E \cdot \log V)$，其中 $E$ 是邊數、$V$ 是節點數

### 備註

-   注意資料型態
-   `vec` 與 `pq` 存的資料是反的喔
-   邊權不可以有負的
