## 快速傅立葉變換 (FFT)

### 用途

求兩多項式相乘的係數

### 用法

1. 不管要幹嘛，先跑 `pre_fft()`
2. 把兩陣列與長度塞入 `mul()`，其中:
    - `a[i]`: 第一個多項式 $x^i$ 項係數, `n` 為其長度
    - `b[i]`: 第二個多項式 $x^i$ 項係數, `m` 為其長度
    - `ans[i]`: 答案的多項式 $x^i$ 項係數

### 備註

-   `MAXN` 一定要是 $2$ 的冪次
-   我不會工數所以我也不清楚細節 By ShanC
