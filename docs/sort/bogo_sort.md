## Bogo 排序法

Defined in [bogo_sort.cpp](/sort/bogo_sort.cpp)

```cpp
void bogo_sort(T arr[], int n);
```

### 參數

-   `T`: 可以比較大小的類別
-   `arr`: 欲排序的陣列
-   `n`: 陣列的大小

### 時間複雜度

-   Average: $O(n \cdot n!)$
-   Worst: $O(\infty)$
-   Best: $O(n)$

### 備註

-   不穩定的排序法
-   不實用的排序法
