## 併查集 Disjoint Set

### 用途

查詢 `x` 和 `y` 是否在同一個集合內。

### 用法

1.  初始化: 宣告 DSU 結構時傳入大小 `n`
2.  找 `x` 的根節點: `find(x)`
3.  合併 `x` 與 `y`: `merge(x, y)`

### 備註

-   注意資料型態
-   啟發式合併 & 路徑壓縮
-   小心弄不好會取到奇怪的記憶體位置
