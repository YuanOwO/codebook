/**
 * @brief 氣泡排序法
 * 時間複雜度 O(N^2)
 *
 * @tparam T 可以比較大小的型別
 * @param arr 欲排序的陣列
 * @param n 陣列的大小
 */

/**
 * @brief 合併排序法
 * 時間複雜度 O(N log N)
 * 陣列區間 [l, r)
 *
 * @tparam T 可以比較大小的型別
 * @param arr 欲排序的陣列
 * @param tmp 用來暫存結果的陣列
 * @param l 陣列的左邊 (包含)
 * @param r 陣列的右邊 (不包含)
 */

/**
 * @brief 快速排序法
 * 時間複雜度 O(N log N)
 * 陣列區間 [l, r)
 *
 * @tparam T 可以比較大小的型別
 * @param arr 欲排序的陣列
 * @param l 陣列的左邊 (包含)
 * @param r 陣列的右邊 (不包含)
 */

/**
 * @brief 選擇排序法
 * 時間複雜度 O(N^2)
 *
 * @tparam T 可以比較大小的型別
 * @param arr 欲排序的陣列
 * @param n 陣列的大小
 */
